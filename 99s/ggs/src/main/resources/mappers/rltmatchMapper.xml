<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="rltreply">
 
 		<!-- 댓글보기 -->
    <select id="rltReplyPage" resultType="int">
      SELECT COUNT(*) AS cnt
      FROM reply
      WHERE isvisible=1 AND boardname="rltboard" AND wno=#{writeno}
   </select>
   
    <select id="rltmatchReply" parameterType="com.ggs.DTO.ReplyDTO" resultType="com.ggs.DTO.ReplyDTO">
      SELECT *
      FROM reply
      WHERE boardname="rltboard" AND isvisible=1 AND wno=#{wno}
      ORDER BY repno DESC
      LIMIT #{start}, 10
   </select>
  
 		<!-- 댓글 쓰기 -->
   <insert id="rltreplyAdd" parameterType="com.ggs.DTO.ReplyDTO">
      INSERT INTO reply(wno, boardname, id, content, writedate)
      VALUES (#{wno},"rltboard",#{id},#{content}, now())
   </insert>


</mapper>
