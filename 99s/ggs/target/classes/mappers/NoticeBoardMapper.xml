<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="NoticeBoard">
	
	<!-- 전체게시물 수 조회 -->
	<select id="totalCnt" resultType="java.lang.Integer">
		 select count(*) from notice
		 where isvisible=1
	</select>



  <!-- 게시판 목록 -->
   <select id="listView" 
  
   resultType="com.ggs.notice.NoticeBoardDTO">
     select
      writeno,n.id,title,content,writedate,views,isvisible,imagesrc,boardname
     from notice as n
     Join members as m
     on n.id=m.id
   </select>
	
	
</mapper>	








   
   
   